# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=26.2
loader_version=0.19.3
loom_version=1.17-SNAPSHOT

# Mod Properties
version=1.0.0
group=ws.siri.jscore

# Dependencies
# The Graal version is NOT declared here. It is derived at configuration time from whatever
# the jscore build below was compiled against, so the two mods can never end up on
# mismatched Truffle/js-language builds. See build.gradle.kts.
jscore_group=ws.siri.jscore
jscore_name=jscore
jscore_version=1.0.0
